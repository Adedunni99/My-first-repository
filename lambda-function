import json

def lambda_handler(event, context):
    print("hello world from lambdafunction")

    b=3+5
    print(b)
    return {
        'statusCode': 200,
        'body': json.dumps('Hello from lambda!')
    }
